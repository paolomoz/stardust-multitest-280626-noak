# Direction — Paramount

## Active (resolved 2026-06-28)

**Phrase:** "faithful redesign, modernized — single canonical direction, no approval gate"

**Restatement:** A brand-faithful refresh of paramount.com that preserves the
captured identity (deep-navy palette, Peak Sans / Paramount Vista Sans, mountain
emblem, signature film-montage motion) and lifts execution to a current bar.

**Mode:** A (brand-faithful). Brand signal = `signal-strong` (palette ≥ 3 distinct
colors after clustering; named heading + body families). No `--rebrand`; phrase is
migration-shaped ("faithful", "modernized").

**Movements:**
- expressive: restrained → committed (lead with moving image, cinematic scale)
- distinctiveness: familiar → distinctive (own the navy stage + accent sparks)
- tone: institutional, held; add portfolio playfulness via accent markers
- density: balanced (brand-register multi-audience hard floor; sectionPadding 64px)
- ia-fidelity: reimagined (default — brand list → mark-led grid; video hero)
- palette / type: PINNED (Mode A)

**Divergence (brand-faithful):**
- decade ✓ 2025-now · register ✓ cinematic-media · ground-family inherited (deep-navy)
- font deck inherited · palette inherited
- motion register: kinetic-display (display-typography-signature + signage-led)

**Single variant** (no N>1 fork requested). Improvements list:
`stardust/prototypes/index-improvements.md` (5 items).

**Questions:** none asked — fully autonomous run, defaults applied (density balanced,
ia-fidelity reimagined).

**Commands proposed:** prototype (home first, then one per template) → migrate →
deploy → rollout.

**Confirmation:** auto-confirmed (no-gate run per assignment).

**Wrote:** PRODUCT.md, DESIGN.md, DESIGN.json, index-improvements.md.
