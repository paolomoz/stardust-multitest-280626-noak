# Direction — PayPal (resolved)

## Active

**Phrase:** "Faithful redesign, modernized."

**Mode:** A (brand-faithful). Brand signal = **signal-strong** (9-color palette,
named type families PayPal Pro/Plain). No rebrand trigger → palette + type pinned.

**Register:** brand (multi-audience: consumer / business / enterprise + help).

### Movements
- expressive: restrained → committed (bolder display, weight 800, ratio 1.333)
- distinctiveness: familiar → distinctive (layered cards, sky accents amplified)
- tone: held (trust-forward, benefit-led)
- density: **balanced** (multi-audience hard floor → sectionPadding ≤ 64px desktop)
- ia-fidelity: **reimagined-light** — IA preserved, surface modernized
- palette: **inherited** (PayPal navy/blue/sky/gold)
- type: **inherited** (brand fallback stack, no external font → fast LCP)

### Divergence (brand-faithful mode)
- ground-family   inherited → stark-white (brand-native)
- font deck       inherited → existing site stack (Helvetica Neue/Arial)
- palette         inherited → captured 9-color set
- decade/register rolled inside Mode A → 2025-now, product/transactional
- brand-faithful inversions: all-caps eyebrows → mixed-case; competing CTAs → one primary + text link

### Variants
Single canonical direction (no N>1, no approval gate per run config).

### Command sequence
extract ✓ → direct ✓ → prototype (home + 8 templates) → migrate → deploy → rollout

### Provenance
Resolved 2026-06-28 from stardust/current/_brand-extraction.json (9 archetypes).
Autonomous run; no user gate.
