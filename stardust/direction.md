# Direction — Sony Group Portal

## Active (resolved 2026-06-28)

**Phrase:** "faithful redesign, modernized"

**Restatement:** Migrate the Sony Group corporate portal to EDS, preserving brand identity and IA while modernizing execution.

**Register:** brand (inherited). Multi-audience corporate portal.

**Mode:** A — brand-faithful. Captured signal `signal-strong` (palette black/white/grey + blue + red; named type family SST). No rebrand trigger; `--rebrand` not passed.

**Movements:**
- expressive: restrained → committed (sharper hierarchy, fuller imagery)
- distinctiveness: familiar (kept — Sony minimalism is the identity)
- tone: serious (kept — corporate editorial)
- density: balanced (multi-audience hard floor → sectionPadding 64px desktop)
- ia-fidelity: verbatim (faithful; reproduce IA, modernize surface)

**Divergence (brand-faithful mode):**
- decade        ✓ rolled    → 2025-now
- register      ✓ rolled    → editorial
- ground-family inherited   → stark-white (brand-native)
- font deck     inherited   → SST stack, substituted Inter (SST unlicensed)
- palette       inherited   → near-monochrome + one accent blue

**Brand-faithful inversions:** pure #000→#0a0a0a ink, #fff retained, SST→Inter. (See DESIGN.json.)

**Signature preservation:** home hero auto-rotating image carousel reproduced (cross-fade, ~6s, reduced-motion holds first slide).

**Image-reuse contract:** captured Sony imagery reused at same semantic position via public URLs.

**Motion register:** editorial (rationale: PRODUCT.md publication-register personality).

**Command sequence:** extract ✓ → direct ✓ → prototype (home + per-template) → migrate → deploy/rollout.

**Confirmation:** autonomous run, no gate (per assignment).

**Templates in scope:** home, section-landing (×7), listing (×2), article (×3), editorial (×2).

**Wrote:** PRODUCT.md, DESIGN.md, DESIGN.json, stardust/prototypes/en-improvements.md.
