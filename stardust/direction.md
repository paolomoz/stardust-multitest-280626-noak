# Direction — Xfinity

## Active

**Phrase (verbatim):** "faithful redesign, modernized — keep Xfinity brand DNA (purple #5a23b9, XSans/DM Sans, rounded friendly cards), single canonical direction, no approval gate"

**Restatement:** A brand-faithful (Mode A) modernization of Xfinity's consumer marketing site. Palette and type are pinned to the captured brand surface; execution is brought to a competent 2026 standard. Single variant. No approval gate (hands-off pipeline).

**Brand signal:** `signal-strong` (≥3 distinct palette colors + named type families DMSans/XSans). Mode A active by default.

**Movements:**
- expressive: restrained → committed (purple promoted to signature accent)
- distinctiveness: familiar → familiar (faithful)
- tone: friendly → friendly (unchanged)
- density: balanced (default; brand-register multi-audience hard floor 64px desktop)
- ia-fidelity: verbatim-leaning (keep IA/section beats; modernize surface)

**Divergence (brand-faithful mode):**
- decade        ✓ rolled    → 2025-now
- register      inherited   → brand
- ground-family inherited   → stark-white (brand-native, Mode C override)
- font deck     inherited   → DM Sans (XSans proprietary → DM Sans 800 substitute)
- palette       inherited   → #141417 / #ffffff / #f6f6f9 / #5a23b9 / #6138f5 / #bda7e3

**Image-reuse contract:** captured Xfinity imagery reused via public URLs at same semantic position; broken/404 images omitted gracefully (never fabricated).

**Gaps / assumptions defaulted:** XSans is proprietary and not web-licensable → substituted with DM Sans 800 for display (noted in DESIGN). No clarifying questions asked (hands-off run).

**Command sequence:** extract ✓ → direct ✓ → prototype (home + per template) → migrate → deploy/rollout.

**Improvements list:** stardust/prototypes/index-improvements.md (5 items).

**Confirmation:** auto-confirmed (no-gate hands-off run).
