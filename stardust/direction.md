# Direction — Sycamore Partners

## Active

**Phrase:** "Faithful redesign, modernized." (autonomous production migration)

**Restatement (dimensional):** Keep brand identity (palette, type, motifs, voice);
move *execution quality* and *era* forward (restrained → restrained-but-current),
remove dated patterns (splash redirect, framework chrome). No rebrand.

**Mode:** A (brand-faithful). Captured signal = **signal-strong** (8-color palette,
two named type families). No rebrand trigger → Mode A default.

**Movements:**
- decade: rolled → 2025-now
- expressive: restrained (held)
- distinctiveness: familiar → recognizably-itself (held)
- tone: institutional (held)
- density: balanced (default; brand-register multi-audience hard floor → ≤64px)
- ia-fidelity: reimagined-lite — same IA/section beats preserved; surface modernized
- register: brand (inherited)

**Divergence (brand-faithful mode):**
- decade        ✓ rolled    → 2025-now
- register      inherited   → brand / institutional
- ground-family inherited   → stark-white (brand-native override)
- font deck     inherited   → Merriweather + Work Sans
- palette       inherited   → forest #4f752d + sage + neutrals

**Signature preservation:** `_brand-extraction.json#heroMedium` (sycamore-tree
video) is reproduced as an inline, non-blocking, reduced-motion-aware hero — locked
IA priority. The forced 4s splash/redirect is removed (brand-faithful inversion).

**Motion register:** `arrival` (civic-formal + institutional).

**Improvements list:** `stardust/prototypes/home-improvements.md` (5 items).

**Variants:** single (N=1). No fork.

**Wrote:** PRODUCT.md, DESIGN.md, DESIGN.json, this file.

**Command sequence:** prototype (home first, then one per template) → migrate →
deploy/rollout.
